import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    input: {
        height: 40,
          margin: 12,
          borderWidth: 1,
          borderRadius: 8,
          borderColor: '#cfcfcf',
          padding: 10,  
    },
    });

    export default styles;