import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 0.5,
  },
  image: {
    width: 150,
    height: 150,
    resizeMode: 'cover',
  },
});

export default styles;
